export const metadata = {
  title: "Profile",
  description: "Next Hero",
};

const ProfilePage = () => {
  return (
    <div>
      <p>Profile Page</p>
    </div>
  );
};

export default ProfilePage;
